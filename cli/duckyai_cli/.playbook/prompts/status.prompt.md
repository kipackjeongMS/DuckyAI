---
mode: agent
description: Show orchestrator and vault status
---

Show a comprehensive status of the DuckyAI system:

1. Call `orchestratorStatus` to check if the daemon is running
2. Call `listAgents` to show available agents
3. Summarize: running state, agent count, next scheduled jobs
